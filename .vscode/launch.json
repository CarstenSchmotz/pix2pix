
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/train.py",
            "args": [
                "--dataroot", "./datasets/output_pix2pix_rgbd",
                "--name", "debugtrain_facades",
                "--model", "pix2pix",
                "--direction", "AtoB",
                "--gpu_ids", "-1",
                "--display_id", "0",
                "--preprocess", "none"
            ],
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}